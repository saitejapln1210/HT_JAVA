# Core Java Package 1 Interview Questions

- What is the difference between JDK, JRE, and JVM?
- Why does Java compile to bytecode instead of machine code directly?
- What is a class in Java, and how is it different from an object?
- Which modifiers are valid for a top-level class, and why are `private` and `protected` not allowed?
- What is the difference between primitive and reference data types?
- Why do `float` literals need `f` in Java?
- How does operator precedence differ from associativity?
- What is the default value of instance fields and static fields in Java?
- What is the difference between `private`, default, `protected`, and `public`?
- Can a `final` field be initialized inside a constructor?
- What is the difference between `static` and instance members?
- Can a static method access instance members directly?
- Why can a `final` method not be overridden?
- What happens if two objects update the same static field?
- When should you prefer `static final` constants?
