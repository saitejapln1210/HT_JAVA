# Core Java Package 2 Interview Questions

- What is a method signature in Java?
- Why is return type not part of a method signature?
- What is method overloading, and why is it called compile-time polymorphism?
- How does the compiler resolve overloaded methods?
- What is the promotion order used in method overloading?
- Why does exact match get priority over promoted types?
- How does Java choose between `Object` and `String` overloads?
- Why can `null` create ambiguity in method overloading?
- When does var-args participate in overload resolution?
- Why is var-args considered lowest priority?
- What happens when a parent reference points to a child object in overloaded method calls?
- How is method overloading different from method overriding?
- Can overloaded methods differ only by return type?
- Can automatic boxing and widening both affect overloading?
- What is the difference between compile-time reference checking and runtime polymorphism?
- Is Java pass by value or pass by reference?
- Why can a method change object state if Java is pass by value?
- What is the difference between changing an object and reassigning an object reference inside a method?
